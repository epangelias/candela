import { ComponentChild } from 'preact';

export function TabbedView(props: { children: (typeof Tab)[] }) {
  return <></>;
}

export function Tab(props: { children: ComponentChild }) {
  return <></>;
}
